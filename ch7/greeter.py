#!/usr/bin/env python
# greeter.py: input() function
# 20 Aug 2018 | fjgl
prompt = "If you tell us who you are, we can personalize the messages you see."
prompt += "\nWhat is your name? "
name = input(prompt)
print("Hello, " + name + "!")

# input in input() function is always string
